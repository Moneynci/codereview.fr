version: 0.0
os: linux
files:
  - source: /
    destination: /
hooks:
  ApplicationStart:
    - location: sudo docker-compose up -d
      timeout: 300
      runas: root
  ApplicationStop:
    - location: sudo docker-compose down
      timeout: 300
      runas: root
